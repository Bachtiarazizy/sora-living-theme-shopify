<section class="account-page page-width">
  <h1>Addresses</h1>
  {%- for address in customer.addresses -%}
    <div class="address-card">{{ address | format_address }}</div>
  {%- endfor -%}
  <p><a href="{{ routes.account_url }}">Back to Account</a></p>
</section>
{% schema %}{ "name": "Addresses", "settings": [] }{% endschema %}
